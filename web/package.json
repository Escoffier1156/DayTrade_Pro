{
  "name": "daytrade-pro-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "DayTrade Pro のダッシュボード。Python側は依存ゼロのまま、フロントだけ npm を使う。",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": { "lightweight-charts": "^5.0.0" },
  "devDependencies": { "typescript": "^5.6.0", "vite": "^5.4.0" }
}
